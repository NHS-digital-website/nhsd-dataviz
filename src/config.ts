type Config = {
    padding: string,
    desktopViewport: number,
    defaultWidth: number,
};  

const defaultConf: Config = {
    padding: '2em',
    desktopViewport: 1024,
    defaultWidth: 300,
};

export default defaultConf;