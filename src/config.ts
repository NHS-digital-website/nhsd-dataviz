type Config = {
    padding: string,
    desktopViewport: string,
    defaultWidth: number,
};  

const defaultConf: Config = {
    padding: '2em',
    desktopViewport: '1024px',
    defaultWidth: 300,
};

export default defaultConf;